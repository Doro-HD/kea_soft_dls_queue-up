apiVersion: apps/v1
kind: Deployment
metadata:
  name: qu-admin-guest-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qu-guest-backend
  template:
    metadata:
      labels:
        app: qu-guest-backend
    spec:
      containers:
        - name: guest-backend
          image: qu-guest-backend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
              hostPort: 5173

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: qu-guest-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qu-guest-frontend
  template:
    metadata:
      labels:
        app: qu-guest-frontend
    spec:
      containers:
        - name: guest-frontend
          image: qu-guest-frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
              hostPort: 8000

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: qu-guest-write-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qu-guest-write-db
  template:
    metadata:
      labels:
        app: qu-guest-write-db
    spec:
      containers:
        - name: guest-write-db
          image: mysql:8
          ports:
          - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: 'fireflux'

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: qu-guest-read-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qu-guest-read-db
  template:
    metadata:
      labels:
        app: qu-guest-read-db
    spec:
      containers:
        - name: guest-read-db
          image: mongo:8
          ports:
          - containerPort: 27017

---
